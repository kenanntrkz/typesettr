%% ═══════════════════════════════════════════════════════════
%% TYPESETTR — article-minimal template
%% Style: Minimal article (no numbering, centered, clean)
%% ═══════════════════════════════════════════════════════════

\documentclass[{{FONT_SIZE}},oneside]{article}

%% ── Encoding & Language ──────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[{{BABEL_LANG}}]{babel}

%% ── Font ─────────────────────────────────────────────────
{{FONT_PACKAGE}}

%% ── Page Geometry ────────────────────────────────────────
\usepackage[{{PAPER_SIZE}},top={{MARGIN_TOP}},bottom={{MARGIN_BOTTOM}},left={{MARGIN_LEFT}},right={{MARGIN_RIGHT}}]{geometry}

%% ── Line Spacing ─────────────────────────────────────────
\usepackage{setspace}
{{LINE_SPACING_CMD}}

%% ── Microtypography ─────────────────────────────────────
\usepackage{microtype}

%% ── Headers & Footers (very minimal) ────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyfoot[C]{\small\thepage}%
  \renewcommand{\headrulewidth}{0pt}%
}

%% ── Section Title Styles (Minimal: no number, centered) ─
\usepackage{titlesec}

%% No numbering on sections
\setcounter{secnumdepth}{-1}

\titleformat{\section}[block]
  {\normalfont\LARGE\bfseries\centering}
  {}
  {0pt}
  {}
\titlespacing*{\section}{0pt}{40pt}{20pt}

\titleformat{\subsection}
  {\normalfont\large\bfseries\centering}{}{}{}
\titlespacing*{\subsection}{0pt}{20pt}{10pt}

\titleformat{\subsubsection}
  {\normalfont\normalsize\itshape\centering}{}{}{}
\titlespacing*{\subsubsection}{0pt}{15pt}{8pt}

%% ── Table of Contents (minimal, no numbers) ─────────────
\usepackage{tocloft}
\renewcommand{\cftsecfont}{\normalfont}
\renewcommand{\cftsubsecfont}{\normalfont\itshape}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\setlength{\cftbeforesecskip}{6pt}
\setlength{\cftbeforesubsecskip}{2pt}
\renewcommand{\cftsecpresnum}{}
\renewcommand{\cftsecaftersnum}{}
\setlength{\cftsecnumwidth}{0em}

%% ── Graphics & Floats ────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage[font=small,labelfont=it,justification=centering]{caption}
\graphicspath{{images/}}

%% ── Tables ───────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}

%% ── Lists ────────────────────────────────────────────────
\usepackage{enumitem}
\setlist{nosep,leftmargin=1.5em}

%% ── Quotes ───────────────────────────────────────────────
\usepackage{csquotes}

%% ── Footnotes ────────────────────────────────────────────
\usepackage[bottom,hang]{footmisc}
\setlength{\footnotemargin}{0.8em}

%% ── Index (optional) ─────────────────────────────────────
{{INDEX_SETUP}}

%% ── Bibliography (optional) ──────────────────────────────
{{BIBLIOGRAPHY_SETUP}}

%% ── PDF & Hyperlinks (LAST) ─────────────────────────────
\usepackage[
  colorlinks=false,
  hidelinks,
  bookmarks=true,
  pdfstartview=FitH
]{hyperref}
\hypersetup{
  pdftitle={{{PDF_TITLE}}},
  pdfauthor={{{PDF_AUTHOR}}}
}
\usepackage{bookmark}

%% ── Title Data ───────────────────────────────────────────
\title{{{BOOK_TITLE}}}
\author{{{BOOK_AUTHOR}}}
\date{{{BOOK_DATE}}}

%% ═════════════════════════════════════════════════════════
%% END OF PREAMBLE
%% ═════════════════════════════════════════════════════════
