%% ═══════════════════════════════════════════════════════════
%% TYPESETTR — exam-modern template
%% Style: Modern exam/test paper (color accents, contemporary)
%% Parametric placeholders: {{VARIABLE}}
%% ═══════════════════════════════════════════════════════════

\documentclass[{{FONT_SIZE}},oneside]{article}

%% ── Encoding & Language ──────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[{{BABEL_LANG}}]{babel}

%% ── Font ─────────────────────────────────────────────────
{{FONT_PACKAGE}}

%% ── Page Geometry (compact for exams) ────────────────────
\usepackage[{{PAPER_SIZE}},top=15mm,bottom=15mm,left=20mm,right=20mm]{geometry}

%% ── Line Spacing ─────────────────────────────────────────
\usepackage{setspace}
{{LINE_SPACING_CMD}}

%% ── Microtypography ─────────────────────────────────────
\usepackage{microtype}

%% ── Color (for modern accents) ──────────────────────────
\usepackage{xcolor}
\definecolor{chaptercolor}{HTML}{2C3E50}
\definecolor{rulecolor}{HTML}{BDC3C7}
\definecolor{accentcolor}{HTML}{2980B9}
\definecolor{headerblue}{HTML}{1A5276}

%% ── Last Page (for page count in footer) ─────────────────
\usepackage{lastpage}

%% ── Headers & Footers ────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{headerblue}\textbf{{{EXAM_SCHOOL}}}}
\fancyhead[R]{\small\color{chaptercolor}{{EXAM_DATE}}}
\fancyfoot[C]{\small\color{chaptercolor}Sayfa \thepage/\pageref{LastPage}}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

%% First page style
\fancypagestyle{plain}{%
  \fancyhf{}%
  \fancyfoot[C]{\small\color{chaptercolor}Sayfa \thepage/\pageref{LastPage}}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0pt}%
}

%% ── Exam Header Block (Modern styled) ───────────────────
\newcommand{\examheader}{%
  \begin{center}
    {\color{accentcolor}\rule{\textwidth}{2pt}}\[10pt]
    {\Large\bfseries\color{headerblue} {{EXAM_SCHOOL}}}\[8pt]
    {\large\bfseries\color{chaptercolor} {{EXAM_TITLE}}}\[6pt]
    {\normalsize\color{chaptercolor} {{EXAM_DATE}}}\[10pt]
    {\color{accentcolor}\rule{\textwidth}{2pt}}
  \end{center}
  \vspace{4mm}
}

%% ── Section Style (for questions — unnumbered) ──────────
\usepackage{titlesec}
\setcounter{secnumdepth}{-1}

\titleformat{\section}[block]
  {\normalfont\large\bfseries\color{headerblue}}
  {}
  {0pt}
  {}
  [\vskip2pt{\color{rulecolor}\rule{\textwidth}{0.5pt}}]
\titlespacing*{\section}{0pt}{16pt}{8pt}

\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{chaptercolor}}{}{}{}
\titlespacing*{\subsection}{0pt}{10pt}{4pt}

%% ── Graphics & Floats ────────────────────────────────────
\usepackage{graphicx}
\usepackage{float}
\usepackage[font=small,labelfont={bf,color=chaptercolor},justification=centering]{caption}
\graphicspath{{images/}}

%% ── Tables ───────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}

%% ── Lists ────────────────────────────────────────────────
\usepackage{enumitem}
\setlist{nosep,leftmargin=1.5em}
\setlist[itemize]{label={\color{accentcolor}\textbullet}}

%% ── Quotes ───────────────────────────────────────────────
\usepackage{csquotes}

%% ── Footnotes ────────────────────────────────────────────
\usepackage[bottom,hang]{footmisc}
\setlength{\footnotemargin}{0.8em}

%% ── Signature Block ──────────────────────────────────────
\newcommand{\signatureblock}{%
  \vfill
  {{EXAM_SIGNATURE_BLOCK}}
}

%% ── Index (optional) ─────────────────────────────────────
{{INDEX_SETUP}}

%% ── Bibliography (optional) ──────────────────────────────
{{BIBLIOGRAPHY_SETUP}}

%% ── PDF & Hyperlinks (LAST) ─────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=chaptercolor,
  citecolor=chaptercolor,
  urlcolor=accentcolor,
  bookmarks=true,
  bookmarksnumbered=true,
  pdfstartview=FitH
]{hyperref}
\hypersetup{
  pdftitle={{{PDF_TITLE}}},
  pdfauthor={{{PDF_AUTHOR}}}
}
\usepackage{bookmark}

%% ── Title Data ───────────────────────────────────────────
\title{{{BOOK_TITLE}}}
\author{{{BOOK_AUTHOR}}}
\date{{{BOOK_DATE}}}

%% ═════════════════════════════════════════════════════════
%% END OF PREAMBLE — Content begins after \begin{document}
%% ═════════════════════════════════════════════════════════
